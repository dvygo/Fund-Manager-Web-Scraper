# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Pipeline output is committed (public-source data) — kept in the repo.

# Local secrets
docker/.env

# caveman-compress backups
*.original.md

# Crawl4AI / Playwright local caches
.crawl4ai/

# OS / editor
.DS_Store
Thumbs.db


# Frontend lives in its own repo: github.com/dvygo/wealth-managers-internal
# (src/build_web_data.py still writes its data shards into web/public/data/)
web/
