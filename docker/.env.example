# Copy to .env in this folder and set real values before `docker compose up`.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-me

# Sized for 6GB VRAM (RTX 3050): 3B params, AWQ 4-bit ~1.9GB weights.
# Too tight? Drop to Qwen/Qwen2.5-1.5B-Instruct-AWQ.
# Bigger GPU later? Qwen/Qwen3-8B + VLLM_MAX_MODEL_LEN=8192.
VLLM_MODEL=Qwen/Qwen2.5-3B-Instruct-AWQ
VLLM_MAX_MODEL_LEN=2048

# Only needed for gated HuggingFace models.
HF_TOKEN=

# Phase 4 LinkedIn search.
# Recommended: Brave Search API — reliable, free tier ~2000/mo covers the
# roster with no throttling. Get a key: https://brave.com/search/api/
BRAVE_API_KEY=
# Fallback if no Brave key: self-hosted SearXNG (flaky — engines throttle its
# scrapes). Start it with `docker compose up -d searxng tor`.
SEARXNG_URL=http://localhost:8080
SEARCH_GAP_SECONDS=4        # pause between live SearXNG queries
# HUNTER_API_KEY=          # optional: verified fund-manager emails
